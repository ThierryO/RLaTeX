options(
  papersize = "a4",
  tab.width = 2,
  width = 80,
  help_type = "html",
  stringsAsFactors = TRUE,
  keep.source.pkgs = TRUE,
  xpinch = 300,
  ypinch = 300,
  repos = c(
    RStudio = "https://cloud.rstudio.com/",
    INLA = "https://inla.r-inla-download.org/R/stable"
  )
)
